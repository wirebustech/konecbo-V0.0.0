apiVersion: v1
kind: Service
metadata:
  name: konecbo-service
  labels:
    app: konecbo
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: konecbo

